'use strict';

// const ReviewStorage = require('./ReviewStorage');

class Review {
  constructor(body) {
    this.body = body;
    this.params = params;
  }

  createByClubNum() {
    const review = this.body;
    console.log(review);
  }
}

module.exports = Review;
